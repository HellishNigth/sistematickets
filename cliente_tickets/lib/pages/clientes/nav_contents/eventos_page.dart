import 'package:flutter/material.dart';

class EventosClientesPage extends StatelessWidget {
  const EventosClientesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('CHUPALO POR 2'),
    );
  }
}
