import 'package:flutter/material.dart';

class MiTicketsPage extends StatelessWidget {
  const MiTicketsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('CHUPALO BENJA KLO'),
    );
  }
}
